//! Debian-specific helpers (scaffold).

use crate::AphelionError;

/// Fetch Debian-specific repository data (placeholder).
pub fn fetch_repo_data() -> Result<(), AphelionError> {
    Ok(())
}

/// Install a Debian package (placeholder).
pub fn install_debian_package(_package_name: &str) -> Result<(), AphelionError> {
    Ok(())
}
